{
  "name": "geroenok-lib",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Общая логика книги (текст, промпты, типографика) для Cloudflare Worker в ../worker и её тесты",
  "scripts": {
    "test": "node --test test/*.test.js ../worker/test/*.test.js"
  }
}
